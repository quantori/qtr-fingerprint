pipeline {
  agent {
    docker {
      image 'rikorose/gcc-cmake:latest'
      label 'qtr'
    }
  }
  stages {
    stage('build') {
      steps {
        sh 'pwd && ls -lha'
        sh 'mkdir build && cd build'
        sh 'pwd && ls -lha'
        sh 'cmake ..'
        sh 'cmake --build .'
      }
    }
    stage('test') {
      steps {
        sh './bin/tests'
      }
    }
  }
}
