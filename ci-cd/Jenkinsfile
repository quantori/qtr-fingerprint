pipeline {
  agent {
    docker {
      image 'rikorose/gcc-cmake:latest'
      label 'qtr'
    }
  }
  stages {
    stage('build') {
      steps {
        sh 'mkdir build && cd build'
        sh 'cmake ..'
        sh 'cmake --build .'
      }
    }
    stage('test') {
      steps {
        sh './bin/tests'
      }
    }
  }
}
